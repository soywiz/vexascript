console.log([1,2,3,4,5,6].map(it => it * 2).filter(it => it % 3 == 0))
console.log([1,2,3,4,5,6].map(it => it * 2).filter(it => it % 3 == 0))
console.log([1,2,3,4,5,6].filter(value => value % 2 == 0).map(value => value * value))
console.log(["a", "b", "c"].map((value, index) => `${index}:${value}`).join(","))
console.log([1,2,3,4].reduce((acc, value) => acc + value, 0))
console.log([1,2,3,4,5].some(it => it > 4))
console.log([1,2,3,4,5].every(it => it < 10))
console.log([1,2,3,4,5].find(it => it % 2 == 0))
console.log([1,2,3].map(value => [value, value * 10]))
