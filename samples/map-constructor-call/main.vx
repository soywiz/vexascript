// Constructor-only ECMAScript globals remain constructable when their ambient
// interface declarations are merged with newer static helper declarations.
const scores = Map<string, number>([["Ada", 3], ["Grace", 5]])
scores.set("Linus", 8)

console.log(scores instanceof Map)
console.log(scores.get("Grace"))
console.log([...scores.keys()].join(","))
