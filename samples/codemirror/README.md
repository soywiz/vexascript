# CodeMirror sample

The Node entry exercises CodeMirror's immutable editor state, generic state
effects and fields, transaction specifications, change sets, and document
accessors. The browser entry exercises the root `codemirror` re-exports,
language extensions, DOM construction, dispatch overloads, and real rendering.
