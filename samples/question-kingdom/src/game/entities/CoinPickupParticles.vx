import { Container } from 'pixi.js';
import { GameObject } from '../core/GameObject.vx';
import type { GameScene } from '../core/GameScene.vx';
import { PIXELS_PER_METER } from '../core/constants.vx';
import { CoinParticleEmitter } from '../scripts/CoinParticleEmitter.vx';

export class CoinPickupParticles extends GameObject {
  constructor(scene: GameScene, x: number, y: number) {
    super(scene, 'Coin Pickup Particles', Container());
    view.position.set(x * PIXELS_PER_METER, y * PIXELS_PER_METER);
    addComponent(new CoinParticleEmitter());
  }
}
