# Drizzle ORM sample

Exercises the PostgreSQL table/column builder chain, inferred table metadata,
mapped column extraction, SQL expression composition, and package subpath
imports using Drizzle ORM 0.45.2.
