# Effect sample

This sample exercises Effect's namespace reexports, higher-order generic
operators, `pipe` overloads, and asynchronous effect execution.

Run it from the repository root with:

```sh
pnpm cli vexa samples/effect/main.vx
```
