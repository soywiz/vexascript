# TanStack Table sample

This sample exercises TanStack Table's recursive `ColumnDef` unions, generic
table options, row models, and preservation of the original row type.

Run it from the repository root with:

```sh
pnpm cli vexa samples/tanstack-table/main.vx
```
