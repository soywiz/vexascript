# XState sample

This sample exercises XState's generic machine, actor, snapshot, transition,
and `assign` declarations using the unmodified published package.

Run it from the repository root with:

```sh
pnpm cli vexa samples/xstate/main.vx
```
