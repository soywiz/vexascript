import { render } from "preact";
import { App } from "./src/App.vx";

const root = document.getElementById("app");
if (!root) throw Error("Missing #app root");

render(<App />, root);
console.log("preact-advanced-sample-ready");
